<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Borgarverk.EntryListPage">
	<ContentPage.Content>
		<ListView ItemsSource="{Binding Entries}">
			 <ListView.Header>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="0.2*" />
						<ColumnDefinition Width="0.4*" />
						<ColumnDefinition Width="0.2*" />
						<ColumnDefinition Width="0.2*" />
					</Grid.ColumnDefinitions>
					<Label Text="Bíll" FontAttributes="Bold" />
						<Label Grid.Column="1" Text="Staður" />
						<Label Grid.Column="2" Text="Gerð"/>
						<Label Grid.Column="3" Text="Sent"/>
				</Grid>
			</ListView.Header>
			<ListView.ItemTemplate>
     			<DataTemplate>
       				<ViewCell>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="0.2*" />
								<ColumnDefinition Width="0.4*" />
								<ColumnDefinition Width="0.2*" />
								<ColumnDefinition Width="0.2*" />
							</Grid.ColumnDefinitions>
							<Label Text="{Binding Car}" FontAttributes="Bold" />
							<Label Grid.Column="1" Text="{Binding Station}" />
							<Label Grid.Column="2" Text="{Binding TimeCreated}"/>
							<Label Grid.Column="3" Text="{Binding TimeSent}"/>
						</Grid>

						<!--<ViewCell.View>
           					<StackLayout Padding="10,5,0,5" Orientation="Horizontal">
        						<Label Text="{Binding Car}" />
        						<Label Text="{Binding Station}"/>
								<Label Text="{Binding TimeCreated}"/>
								<Label Text="{Binding TimeSent}" />
    						</StackLayout>
        				</ViewCell.View>-->
						
        			</ViewCell>
     			</DataTemplate>
   			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>
